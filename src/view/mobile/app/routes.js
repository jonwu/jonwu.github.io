import Home from 'view/mobile/home/Home';
import resume from 'view/global/assets/resume.pdf';
const routes = [
  {
    path: '/',
    component: Home,
    loadData: () => {},
  },
  
]

export default routes;
